name: Azure Full Stack App Deployment

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install frontend dependencies
      working-directory: ./angular-frontend
      run: npm install

    - name: Build Angular frontend
      working-directory: ./angular-frontend
      run: npm run build -- --configuration production

    - name: Copy Angular build to backend public folder
      run: |
        rm -rf ./node-backend/dist
        mkdir -p ./node-backend/dist/angular-frontend
        cp -r ./angular-frontend/dist/angular-frontend/* ./node-backend/dist/angular-frontend/

    - name: Install backend dependencies
      working-directory: ./node-backend
      run: npm install

    - name: Deploy to Azure App Service
      uses: azure/webapps-deploy@v2
      with:
        app-name: CelesContainerWebApp  # Replace with your Azure Web App name
        slot-name: sample
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        package: ./node-backend
